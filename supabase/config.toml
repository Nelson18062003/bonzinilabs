project_id = "fmhsohrgbznqmcvqktjw"

# generate-flyer serves public rate data — no auth needed.
# Without verify_jwt = false, Supabase rejects the CORS OPTIONS preflight
# with a 401 before the function code is even reached.
[functions.generate-flyer]
verify_jwt = false

# resend-events receives Resend webhooks (external, no Supabase JWT).
# Authenticity is enforced via Svix signature verification inside the function.
[functions.resend-events]
verify_jwt = false

# passkey sert les routes login/start et login/finish AVANT toute session :
# c'est tout l'intérêt d'une clé d'accès. Le JWT des routes d'enrôlement
# (register/*) est donc vérifié À LA MAIN dans la fonction — cf. callerFromJwt().
[functions.passkey]
verify_jwt = false

# send-email is driven by pg_cron via pg_net (no Supabase user JWT).
# Authenticity is enforced via the mandatory EMAIL_DRAINER_SECRET bearer check
# inside the function (constant-time compare).
[functions.send-email]
verify_jwt = false

# send-sms is driven by pg_cron via pg_net (no Supabase user JWT), exactly like
# send-email. Authenticity is enforced via the mandatory SMS_DRAINER_SECRET
# bearer check inside the function (constant-time compare, fail-closed).
[functions.send-sms]
verify_jwt = false

# telnyx-webhook receives Telnyx webhooks (external, no Supabase JWT).
# Authenticity is enforced via Ed25519 signature verification against
# TELNYX_PUBLIC_KEY, with a 5-minute replay window — fail-closed if the key
# is not configured.
[functions.telnyx-webhook]
verify_jwt = false