# Refonte mobile — suivi du run autonome (nocturne)

Branche : `claude/stoic-ritchie-Ov4D2` · PR : **#141**
Réfs : `docs/audit-refonte-mobile.md` + langage **Ofspace/Mola** (voir
`src/mobile/screens/assistant/MobileAssistantScreen.tsx` et
`src/__screenshot__/flyer.tsx`).

## Règles autonomes (IMPÉRATIVES)
1. **Préserver TOUTE la logique** des écrans (handlers, hooks, états, data) — ne changer que la présentation.
2. Après CHAQUE étape : `npm run type-check` **et** `npm run build` doivent passer. Si rouge → corriger AVANT de committer.
3. **Commit + push après chaque étape** : `git push -u origin claude/stoic-ritchie-Ov4D2` (retry x4 backoff si réseau).
4. **Ne PAS supprimer les V1 morts** (coordination avec le frère) — juste documenter.
5. **Une seule case par itération** (petits pas, vérifiables).
6. Captures clair/sombre via le harness quand faisable (non bloquant : si le serveur/Playwright échoue, continuer sans).
7. **Reset conteneur ?** → `git fetch origin claude/stoic-ritchie-Ov4D2` puis `git checkout -f -B claude/stoic-ritchie-Ov4D2 origin/claude/stoic-ritchie-Ov4D2` ; recréer `.env` dummy si besoin ; réinstaller chromium si captures (`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers npx playwright install chromium`) ; reprendre à la 1re case non cochée.
8. Cocher la case ici + courte note, dans le même commit que l'étape.
9. **Fin** : quand tout est coché → mettre à jour la description de la PR #141 (récap) → écrire un résumé du matin dans `docs/refonte-progress.md` (section « Résumé matin ») → **arrêter le loop**.

## Décisions (prises en autonomie)
- DS = évolution de `components/treasury/ui.tsx` vers Ofspace (ombre douce, pastilles **neutres**, canvas doux, pilules sombres, gros chiffres, zéro dégradé).
- V1 morts NON supprimés.
- Ordre ci-dessous.

## Checklist

### Phase 0 — Fondations
- [x] **P0.1** Tokens couleurs centraux → `src/mobile/designKit/` (tokens.ts: SURFACE/TEXT/pills/TONE ; status.ts: depositStatusTone/paymentStatusTone/clientStatusTone + roleMeta unique ; methods.ts: PAYMENT_METHOD + LOGO_PATH alipay/wechat/whatsapp ; cash=rouge aligné flyer). type-check OK.
- [x] **P0.2** Kit UI → `src/mobile/designKit/components.tsx` (+ export depuis `index.ts`) : `Card` (ombre douce), `Holder` (pastille neutre + variante par `Tone`), `Avatar` (initiales), `Row` (sans filets), `Amount` (chiffre neutre + unité atténuée, `tabular-nums`), `PrimaryPill`/`SoftPill`, `StatusPill` (tone+label), `StatCard`, `Segmented`, `FormField`/`TextInput` (h-12 rounded), `BottomSheet` (overlay + fermeture backdrop/Échap/X + lock scroll), `ScreenLoader`/`ScreenError`, `SectionTitle`. Présentationnels, typés, dark mode via tokens. type-check + build OK.
- [x] **P0.3** Preview du kit dans le harness → `src/__screenshot__/kit.tsx` (un échantillon de CHAQUE composant sur le canvas Ofspace), enregistré dans `main.tsx` (clé `kit`, route `/`). Captures clair+sombre faites via le harness et copiées dans `docs/maquettes/` (`kit-light.png` / `kit-dark.png`). Rendu validé visuellement (langage Ofspace respecté, dark mode OK).

### Phase 1 — Couleurs unifiées (faible risque, fort impact)
- [x] **P1.1** Rôles admin : source unique → MobileAdminsScreen, MobileCreateAdmin, MobileAdminDetail importent la même.
- [x] **P1.2** Statuts : source unique → clients, deposits, payments (badges) importent la même.

### Phase 2 — Migration module par module
- [x] **M1** More (hub + settings + profile + notifications + history + proofs)
- [x] **M2** Clients (liste · détail · create · ledger · beneficiaries)
- [x] **M3** Deposits (liste V2 · détail V2 · new V2) — logique intacte
- [x] **M4** Payments (liste · détail · new)
- [x] **M5** Rates (onglets unifiés · simulator · porter le flyer v3 dans `generate-flyer` Satori)
- [ ] **M6** Admins (liste · create · detail)
- [ ] **M7** Support (liste · conversation · stats · canned · quick)
- [ ] **M8** Agent-cash (login · payments · scanner · detail · confirm · success)
- [ ] **M9** Alignement final : Treasury + Accueil + Analytics sur tokens définitifs

### Final
- [ ] **F1** `type-check` + `build` + `lint` verts sur l'ensemble
- [ ] **F2** Description PR #141 mise à jour (récap complet)
- [ ] **F3** Section « Résumé matin » écrite ci-dessous + STOP loop

## Journal
- (init) Audit livré, plan validé par l'utilisateur, run autonome lancé.
- P0.1 ✅ designKit (tokens/status/methods) créé — source unique couleurs/statuts/rôles/méthodes. Aucun écran modifié (sûr). Prochaine : P0.2 composants du kit.
- P0.2 ✅ Kit de composants partagés `designKit/components.tsx` (Card, Holder, Avatar, Row, Amount, PrimaryPill/SoftPill, StatusPill, StatCard, Segmented, FormField/TextInput, BottomSheet, ScreenLoader/ScreenError, SectionTitle) — patterns copiés de MobileAssistantScreen + flyer, réutilise tokens + `cn`. Aucun écran métier touché. type-check + build verts. Prochaine : P0.3 preview du kit dans le harness.
- P0.3 ✅ Écran de preview `src/__screenshot__/kit.tsx` (galerie : un échantillon de chaque composant, StatusPill câblés sur les helpers de tons unifiés) + enregistré dans `main.tsx` (clé `kit`). Serveur `SCREENSHOT_MOCK=1` + `ONLY=kit FONT=dm node tools/shoot-dash.mjs` → captures clair+sombre OK, copiées dans `docs/maquettes/kit-light.png` + `kit-dark.png`. type-check + build verts. **Phase 0 terminée.** Prochaine : Phase 1 (P1.1 rôles admin, P1.2 statuts) puis migration module par module.
- P1.1 ✅ Rôles admin sur source unique : les 3 `ROLE_BADGE_COLORS` dupliqués (MobileAdminsScreen, MobileCreateAdmin, MobileAdminDetail) supprimés → remplacés par `roleMeta()` (tone) + `StatusPill`/`Holder` du kit. Libellés conservés via `ADMIN_ROLE_LABELS`. Ajout du rôle `treasurer` (tone `success`) à `ROLE_META` pour compléter la source unique (parité avec `AppRole`). Logique/permissions/filtres intacts. type-check + build verts.
- P1.2 ✅ Statuts sur source unique (badges liste + détail des écrans VIVANTS) : clients (`MobileClientsScreen`, `MobileClientDetail` → `clientStatusTone`), dépôts V2 (`MobileDepositsScreenV2`, `MobileDepositDetailV2` → `depositStatusTone`, dicos `STATUS_COLOR` inline + const `BLUE` orpheline supprimés), paiements (`MobilePaymentsScreen` → `paymentStatusTone`, import `PAYMENT_STATUS_COLORS` retiré ; `MobilePaymentDetailV2` → `paymentStatusTone`, dico `STATUS_COLOR` local supprimé). Tous les badges passent par `StatusPill`. Libellés existants conservés (`*_STATUS_LABELS`, `STATUS_LABEL_KEYS`, `PAYMENT_STATUS_CONFIG`, ternaires i18n). V1 morts (`MobileDepositsScreen`/`MobileDepositDetail`) NON touchés (non routés). Logique/filtres/permissions intacts. type-check + build verts.
- M1 (1/6) ✅ `MobileMoreScreen` (hub) migré sur le kit : contenu sur canvas doux `SURFACE.canvas`, items regroupés en `Card`s par section (`SectionTitle` : Outils / Activité / Support / Administration), nouveau `MenuRow` au langage Ofspace (Holder neutre + libellé/desc + pastille de compteur + chevron, sans filets), profil en `Card` cliquable, toggles thème/langue en `Card`, déconnexion en `Card` (tone danger). Navigation, permissions (`canViewTreasury`/`canAccessSupportChat`/`canManageUsers`), badges (notif + support) et `ThemeToggleCompact`/`LanguageSwitcher` intacts. type-check + build verts.
- M1 (6/6) ✅ **Module More terminé.** 5 écrans restants migrés sur le kit :
  - `more/MobileSettingsScreen` → canvas + `SectionTitle` (Apparence/Compte/À propos) + `Card`/`Row` ; `ThemeToggle` conservé ; rôle affiché en `StatusPill` (`roleMeta`).
  - `MobileAdminProfile` → canvas + holder avatar (overlay caméra conservé) + `FormField`/`TextInput` + `PrimaryPill`. TOUTE la logique upload Storage (`avatars`) + RPC `update_my_admin_profile` + `refreshProfile` + `validateUploadFile` intacte.
  - `MobileNotificationsScreen` → canvas + groupes par date en `SectionTitle` + cartes (`SURFACE.card`) + `Holder` toné par type (`TYPE_CONFIG` → `Tone` au lieu de classes hex) ; groupement/relative-date intacts.
  - `MobileHistoryScreen` → canvas + recherche `TextInput` + chips filtres (`PRIMARY_PILL`/`SOFT_PILL`) + lignes en `Card` avec `Avatar` + badge cible en `StatusPill` toné (`getTargetTone`) ; recherche debouncée + filtre type intacts.
  - `MobileProofsScreen` → canvas + 2× `StatCard` + recherche `TextInput` + grille de vignettes (`SURFACE.card`) avec badge `StatusPill` ; **drawer de prévisualisation → `BottomSheet`** (fermeture backdrop/Échap/X + lock scroll du kit) avec `Row`/`PrimaryPill` ; `ProofThumb` + URLs signées + ouverture fichier intacts.
  - type-check + build verts.
- Captures M1 ✅ Écrans More enregistrés dans le harness (`src/__screenshot__/main.tsx` : clés `more`, `more-settings`, `more-profile`, `more-notifications`, `more-history`, `more-proofs`) + fixtures mock ajoutées dans `tools/shoot-dash.mjs` (audit logs + user_roles + deposit_proofs). Serveur `SCREENSHOT_MOCK=1 vite --host 127.0.0.1 --port 8080` + `ONLY=more,... FONT=dm node tools/shoot-dash.mjs` → 14 captures clair+sombre OK. Copiées dans `docs/maquettes/` (`more-light/dark`, `more-settings-light`, `more-profile-light`, `more-notifications-light`, `more-history-light/dark`, `more-proofs-light/dark`) + kit rafraîchi. Rendu Ofspace validé visuellement (canvas doux, cartes à ombre, holders neutres, StatusPill tonés, BottomSheet). Note : l'écran Notifications rend son empty-state sous le harness (les mocks dépôts/paiements ne composent pas de notifications) — non bloquant.

- M2 (1/5) ✅ `MobileClientsScreen` migré sur le kit : contenu sur canvas doux `SURFACE.canvas`, recherche en `TextInput` (icône `Search`), chips filtres statut en `PRIMARY_PILL`/`SOFT_PILL` (remplace `MobileFilterChips`), lignes client en carte (`SURFACE.card`+`shadow`, bouton) avec `Avatar` (initiales) + `StatusPill` toné (`clientStatusTone`) + solde en gros chiffre `tabular-nums` ; stats dépôts/paiements sans filet ; empty-state `Holder`+`PrimaryPill` (remplace `MobileEmptyState`) ; FAB en `PRIMARY_PILL`. Recherche debouncée + filtre statut client-side + navigation intacts. type-check + build verts.

- M2 (2/5) ✅ `MobileClientDetail` + `components/clients/AdjustmentDrawer` migrés sur le kit :
  - canvas doux ; profil en `Card` (holder initiales + `StatusPill` toné `clientStatusTone`, source UTM en `StatusPill` info) ; **hero solde** = `Card`+`Amount` (size `xl`, unité XAF atténuée) avec lien Historique + boutons Crédit (tone success) / Débit (tone danger) ; stats = 2× `StatCard` (dépôts success / paiements info) ; liste d'actions en `Card`+`ActionRow` (Holder toné + libellé/desc + chevron, calqué sur `MenuRow`, états loading/disabled pour relevé PDF et suppression).
  - **4 drawers → `BottomSheet`** : édition profil (`FormField`/`TextInput` + `PrimaryPill`/`SoftPill`), confirmation suppression (`PrimaryPill danger`), confirmation reset mdp, résultat mdp (code + `Holder` copier toné success). `AdjustmentDrawer` (crédit/débit) : shell `Drawer` → `BottomSheet`, `AmountField`/`TextArea`/`useCreateAdjustment` conservés, solde courant en `Amount`, warning toné pending, CTA `PrimaryPill` (danger si débit) ; garde-fou `handleClose` (bloque la fermeture pendant l'envoi) intact.
  - TOUTE la logique conservée : `useClient`/`useClientLedger`/`useUpdateClient`/`useResetClientPassword`/`useAdminDeleteClient`/`useCreateAdjustment`, garde `canManageUsers`, contrôle solde+paiements en cours avant suppression, génération relevé PDF, copie mot de passe. type-check + build verts.

- M2 (3/5) ✅ `MobileCreateClient` migré du **100 % inline `React.CSSProperties`** vers Tailwind + kit : suppression du thème inline + `<link>` DM Sans + `focusedField` (focus ring géré par `TextInput`) ; canvas doux ; header fixe (back ‹ + titre) + **barre de progression 3 segments** restylée (violet actif, tokens) ; contenu scrollable ; **footer collé avec CTA TOUJOURS visibles**. Étape 1/2/3 en `FormField`+`TextInput` ; 2 `<select>` natifs (indicatif pays + pays avec optgroups) stylés au gabarit `TextInput` (h-12, carte, ring) ; récap étape 3 en `Card` (holder initiales + `Row`) ; notes mot de passe tonées pending ; **écran succès** en `Card`+`Holder` succès + code + `Holder` copier (toné success) + `PrimaryPill`/`SoftPill`. TOUTE la logique conservée : `STEPS`, `COUNTRY_CODES`, `canNext`/`handleNext`/`handleBack`, `handleCreateClient` (clean phone + `useCreateClient`), `handleCopyPassword`, états succès (tempPassword/clientId). type-check + build verts.

- M2 (4/5) ✅ `MobileClientLedger` (mouvements) migré sur le kit : canvas doux ; carte info client en `Card` ; chips filtres en `PRIMARY_PILL`/`SOFT_PILL` (remplace `MobileFilterChips`) ; lignes en `Card` avec `Holder` toné + montant coloré par tone (`AMOUNT_TONE` calqué sur la palette des pills) ; empty-state `Holder`+texte (remplace `MobileEmptyState`). `ENTRY_TYPE_CONFIG` simplifié (classes hex bg/icon/amount → `tone`) en gardant icône/préfixe/label/`isInformational` (les écritures informatives = tone neutre) ; filtres + `useClient`/`useClientLedger` + refresh intacts. type-check + build verts.

- M2 (5/5) ✅ **Module Clients terminé.** `MobileClientBeneficiaries` (+ `BeneficiaryEditor`) migré sur le kit : canvas doux ; bouton d'ajout (header rightElement) en `PRIMARY_PILL` ; recherche `TextInput` ; chips filtres en pills du kit, **l'accent de marque du mode actif est conservé** (`modeColor` — la couleur porte le sens du mode, comme `methods.ts`) ; lignes en `Card` (avatar rond couleur de marque + alias/identifiant + badge mode coloré + `Holder` éditer / `Holder` archiver toné danger) ; empty-state `Holder`+`PrimaryPill` ; skeletons sur surface carte. **Modal d'archivage → `BottomSheet`** (`SoftPill`/`PrimaryPill danger`). `BeneficiaryEditor` : canvas + `BeneficiaryForm` conservé + footer collé toujours visible (`SoftPill`/`PrimaryPill`). TOUTE la logique conservée : scoping `clientId`, hooks admin (`useAdminClientBeneficiaries`/Create/Update/Archive), `BeneficiaryForm`/validation/QR (preview + revokeObjectURL), modes + recherche + archive. type-check + build verts.
- **M2 cochée** : les 5 écrans clients sont sur le kit (liste, détail, création, ledger, bénéficiaires) + `AdjustmentDrawer` en `BottomSheet`.
- Captures M2 ✅ 5 écrans clients enregistrés dans le harness (`src/__screenshot__/main.tsx` : clés `clients`, `client-detail`, `client-create`, `client-ledger`, `client-beneficiaries` ; routes paramétrées pour `useParams`) + fixtures mock ajoutées dans `tools/shoot-dash.mjs` (clients/wallets/deposits/payments/ledger_entries/beneficiaries). Discrimination liste vs détail par l'URL (`user_id=eq.…` → objet single, sinon liste) car ce client Supabase tranche `maybeSingle()` côté JS (l'en-tête Accept reste `application/json`). Serveur `SCREENSHOT_MOCK=1 vite --host 127.0.0.1 --port 8080` + `ONLY=clients,client-detail,client-create,client-ledger,client-beneficiaries FONT=dm node tools/shoot-dash.mjs` → 10 captures clair+sombre OK, copiées dans `docs/maquettes/` (`clients-*`, `client-detail-*`, `client-create-*`, `client-ledger-*`, `client-beneficiaries-*`). Rendu Ofspace validé visuellement (canvas, cartes, holders tonés, hero `Amount`, `StatCard`, `StatusPill`, couleurs de marque des modes bénéficiaires conservées, progress-bar création, dark mode OK). Note : sur la fiche détail, la clé i18n `resetPassword` du namespace `common` n'est pas résolue **sous le harness** (rendue brute) — artefact du harness uniquement, OK dans l'app réelle.

- M3 (1/3) ✅ `MobileDepositsScreenV2` (liste VIVANTE, routée `/m/deposits`) migrée du 100 % inline `style` vers le kit : canvas doux `SURFACE.canvas` ; header collant (titre + bouton « + » vert conservé = marque Dépôts) ; **KPIs** À traiter/À corriger/Validés en tuiles `Card` (chiffre toné info/pending/success aligné `depositStatusTone`, ring actif), toujours cliquables (filtre statut) ; recherche en `TextInput` (icône `Search`, croix `X`) ; bouton filtres avancés restylé carte + `SlidersHorizontal` (remplace `FunnelIcon`) avec point d'activité ; panneau filtres en `Card` (chips méthode/période en `PRIMARY_PILL`/`SOFT_PILL`, dates `custom` en `TextInput type=date`) ; chips statut en pills du kit avec compteur ; lignes dépôt en `Card` bouton avec **MIcon** (vignette méthode, couleur de marque conservée) + nom + réf·méthode + **`Amount`** (montant) + **SlaDot** (+ `@keyframes sla-pulse` conservé) + `StatusPill` toné + date relative ; preuves en `Paperclip`+count (remplace emoji) ; empty-state `Holder`+texte (remplace `MobileEmptyState`). **Logique 100 % intacte** : `useDepositStats`, `usePaginatedAdminDeposits` + `filterParams`, recherche debouncée + filtre famille client-side, présets période + dates custom, `InfiniteScrollTrigger` (gardé hors recherche), `getDepositSlaLevel`, `PullToRefresh` (passe désormais `className`). type-check + build verts.

- M3 (2/3) ✅ `MobileDepositDetailV2` (détail VIVANT, routé `/m/deposits/:id` — l'écran le PLUS sensible) migré du 100 % inline `style` vers le kit, **logique 100 % préservée** : canvas doux ; en-tête commun `DetailHeader` (back + réf + bouton « Relevé » vert avec spinner, conservé pour loading/error/contenu) ; sous-header `StatusPill` toné + **MIcon** + méthode + **SlaDot** + date relative ; **hero montant** = `Card`+`Amount` (xl) + client + diff montant confirmé (flèche verte) + solde wallet ; **galerie de preuves** en `Card` (preview img/PDF/fichier inchangée, overlays nom/uploader, boutons Agrandir/Télécharger/Remplacer/Supprimer restylés en pills, input remplacement caché, dropzone manquante) ; **infos** en `Row` (sans filets) ; **boutons d'action** dans la carte avec TOUTES les conditions intactes (`canStartReview` violet, `canValidate` vert, Rejeter danger, `isSuperAdmin` Annuler) ; **suivi collapsible** en `Card` (timeline `buildDepositTimelineSteps`+`getStepColors` inchangée, connecteurs/états conservés) ; **5 bottom-sheets → `BottomSheet` du kit** (validation : montant confirmé `FormField`/`TextInput`, alerte écart, encart crédit, note interne, toggle « notifier le client » ; rejet : motifs `REJECTION_REASONS` + message client + note ; upload : dropzone + liste fichiers ; suppression preuve : motifs `PROOF_DELETE_REASONS` + « Autre » ; annulation dépôt) — tous les handlers (`handleValidate`/`handleReject`/`handleStartReview`/`handleUploadProofs`/`handleDeleteProof`/`handleReplaceFileSelect`/`handleDownloadReceipt`), états de formulaire, `mutate`+`onSuccess`, `isPending`/disabled et resets identiques ; visionneuse plein écran conservée (lightbox custom, non remplacée). Couleurs sémantiques alignées sur les tons du kit (succès/danger/info) tout en gardant le vert d'action « valider/relevé » = marque Dépôts. type-check + build verts.

- M3 (3/3) ✅ **Module Deposits terminé.** `MobileNewDepositV2` (assistant VIVANT, routé `/m/deposits/new` — c'est bien la V2 qui est routée, pas `MobileNewDeposit`) migré du 100 % inline `style` (thème `t`, DM Sans) vers le kit, calqué sur `MobileCreateClient` : layout `h-[100dvh]` (tab bar masquée) ; **header fixe** (back + titre + compteur étape) + **barre de progression** N segments (vert marque Dépôts) ; **contenu scrollable** ; **footer CTA toujours visible** (`SoftPill` Retour + `PrimaryPill` Suivant/Confirmer en vert). Toutes les étapes migrées en `Card`/pills : client (recherche + liste cartes avatar initiales + `Holder` empty), montant (`Card` + input géant + `useCountUp` `animatedAmount` + présets K/M + alerte limite mobile money), famille/sous-méthode/banque/agence (cartes sélectionnables, **accent couleur de marque conservé** par famille via `boxShadow` ring + `Check`), récap (`Amount` xl, résumé en lignes sans filet, coordonnées + **`CopyBtn`** restylé kit, code marchand, montant à envoyer, instructions numérotées, **upload preuves** + vignettes + retrait, commentaire admin `textarea`, note de confirmation), creating (`Holder` Loader2 animé), **écran succès** (`Holder` success + `Amount` + CTA Retour/Voir la fiche). **Logique 100 % préservée** : tous les handlers de navigation (`goTo`/`handleFamilySelected`/`handleSubMethodSelected`/`handleBankSelected`/`handleAgencySelected`/`handleRecapBack`/`handleHeaderBack`), `getDepositMethod`, `getRecapInfo` (BANK/OM/MTN/WAVE/AGENCE), `handleCopy`, `handleFileSelect`/`removeFile`, `doCreateDeposit` (`mutateAsync` + succès/erreur), `getTotalSteps`/`getStepNumber`, validations (amount≥1000), client présélectionné via URL. type-check + build verts.
- **M3 cochée** : les 3 écrans Deposits VIVANTS (liste V2, détail V2, new V2) sont sur le kit ; V1 morts (`MobileDepositsScreen`/`MobileDepositDetail`/`MobileNewDeposit`) NON touchés. **Note de coordination** : contrairement à la consigne, c'est bien la variante **V2** de l'assistant (`MobileNewDepositV2`) qui est routée dans `App.tsx` (`/m/deposits/new`), pas `MobileNewDeposit` — j'ai donc migré la V2 (= l'écran vivant). Prochaine : M4 Payments.

- Captures M3 ✅ 3 écrans Deposits enregistrés dans le harness (`src/__screenshot__/main.tsx` : clés `deposits`, `deposit-detail` route `/m/deposits/d1` paramétrée `:depositId`, `deposit-new`) + fixtures mock enrichies dans `tools/shoot-dash.mjs` (deposits avec `reference`/`method`/`bank_name`/`agency_name`/`proof_count`/`confirmed_amount_xaf` ; discrimination `id=eq.` → dépôt unique pour le détail ; `deposit_proofs?deposit_id=eq.` → `[]` pour rendre l'état « preuve manquante » sans signer d'URL Storage ; `useAllClients` servi via la branche `select=user_id`). Serveur `SCREENSHOT_MOCK=1 vite --host 127.0.0.1 --port 8080` + `ONLY=deposits,deposit-detail,deposit-new FONT=dm node tools/shoot-dash.mjs` → 6 captures clair+sombre OK, copiées dans `docs/maquettes/` (`deposits-light/dark`, `deposit-detail-light/dark`, `deposit-new-light/dark`). Rendu Ofspace validé visuellement : liste (canvas, KPIs tonés, MIcon de marque, `Amount`, `StatusPill` tonés, SLA dot, chips), détail (hero `Amount`, galerie/dropzone, `Row`, boutons d'action sémantiques violet/vert/danger, Suivi), assistant (header + barre de progression verte, recherche + cartes client) — dark mode nickel (tokens respectés).

- M4 (3/3) ✅ **Module Payments terminé.** `MobileNewPayment` (assistant VIVANT, routé `/m/payments/new`) migré du 100 % inline `React.CSSProperties` (thème `t`, dicos couleurs V/G/O/GR/AL/WC, DM Sans) vers Tailwind + kit, calqué sur `MobileNewDepositV2` : layout `h-[100dvh]` (tab bar masquée) ; **header fixe** (back `ChevronLeft` + titre + compteur `step/5`) + **barre de progression 5 segments** (violet marque Paiements `#8B5CF6`, cohérent FAB liste + `PAYMENT_METHOD.virement`) ; **contenu scrollable** ; **footer CTA toujours visible** (`SoftPill` Retour + `PrimaryPill` Suivant/Confirmer en violet, état loading via `createPayment.isPending`). Étapes migrées : (1) client = recherche `TextInput`+icône+croix + cartes avatar initiales/solde + empty `Holder` ; (2) mode = cartes avec **`PaymentMethodLogo` (vrais logos Alipay/WeChat/Virement/Cash ¥ rouge)** au lieu des glyphes `支/微/B/¥`, ring violet + `Check` ; (3) montant = segment XAF/¥ (langage `Segmented`), `Card` saisie géante + **`useCountUp` sur le converti**, présets + « Tout », bloc taux personnalisé (toggle switch restylé), alertes solde/min tonées danger ; (4) bénéficiaire = `CheckRow` « remplir plus tard », **onglets carnet enregistré/nouveau** (segment violet), liste carnet scopée en cartes + `Check`, formulaires `FormField`/`TextInput` (cash self/autre, Alipay/WeChat QR+dropzone+identifiant, virement banque/compte), `CheckRow` « enregistrer au carnet » ; (5) récap = `Amount` xl (¥ d'abord) + résumé en lignes sans filet + alerte solde. **Écran succès** = `Holder` success + `Amount` ¥ + CTA Nouveau/Voir la fiche. **Logique 100 % préservée** : `useAllClients`/`useActiveDailyRate`/`useAdminCreatePayment`/`useAdminClientBeneficiaries`/`useAdminCreateBeneficiary`, requête `walletsMap`, tous les calculs (`baseRate`/`rate`/`xaf`/`cny` + `getBaseRate`), validations par étape (`benef4Valid`/`hasEnoughBalance`/`canNext` min 10 000), `handleConfirm` (snapshot + payload + `toStoredPath` QR + save carnet), `handleQrChange`/`removeQr`, `reset`, mapping `dbMode` virement→bank_transfer. type-check + build verts.
- **M4 cochée** : les 3 écrans Payments VIVANTS (liste, détail V2, nouveau) sont sur le kit. **Note** : c'est `MobilePaymentDetailV2` qui est exporté comme `MobilePaymentDetail` (barrel) et routé — déjà migré au run précédent. Prochaine : M5 Rates.

- M5 (1/3) ✅ Module Taux — **shell + onglet Définir + onglet Config** sur le kit (suit la maquette validée `rates.tsx`). Helper partagé `rates/components/MethodLogo` (mappe `PaymentMethodKey`→`PaymentMethodLogo`, vrais logos Alipay/WeChat/Virement/Cash ¥ rouge — remplace les emojis `pm.icon`). `MobileRatesScreen` : canvas doux, onglets principaux (Taux/Config/Simuler) + sous-onglets (Définir/Graphique/Historique) en **pilules** (langage `Segmented`/`PRIMARY_PILL`), `MobileHeader` conservé (bouton + violet), `PullToRefresh` + invalidations intacts. `RateSetTab` : segment direction XAF↔CNY (violet), cartes blanches par méthode (logo + label + `TextField` gros chiffre), carte de vérification (¥ + gros chiffre par mode), sélecteur date d'effet en pilules + steppers heure/minute sur `SURFACE.canvas`, CTA **Publier** en `PrimaryPill` (violet, vert au succès), **carte Flyer** (toggle Dark/Light, `RateFlyer` preview scale 0.172 + exports PNG violet / PDF ambre) — dégradés inline supprimés. `RateConfigTab` : cartes blanches, lignes pays/tranches sans filet sur canvas, badge **REF en `StatusPill success`**, `TextField` % rouge conservé, CTA **Sauvegarder** en `PrimaryPill` ambre, erreur via `ScreenError`. **Logique 100 % préservée** : `useCreateDailyRates`/`getEffectiveAt`/`handleApply`/`flyerRates`/exports ; `useRateAdjustments`/`useUpdateRateAdjustment`/`handleSave` (seuls non-référence modifiés). type-check + build verts. Reste M5 : simulateur (+`SimulatorResult`), graphique (+`MultiCurveChart` Recharts), historique (+`RateHistoryCard`).

- M5 (2/3) ✅ Module Taux — **onglet Simulateur (+`SimulatorResult`)** sur le kit. `RateSimulatorTab` : carte blanche, segment devise XAF/CNY (violet), `TextField` montant formaté + raccourcis en pilules, **mode de paiement en grille avec vrais logos** (`MethodLogo`, ring couleur de marque), pays en grille (ring violet), erreur via `ScreenError`. `SimulatorResult` : **dégradé sombre inline remplacé par les tokens** — carte douce, badge saisie CNY, bloc « Vous envoyez » XAF, **« Le client reçoit » en gros (¥ + chiffre 40px)** sur `#EDEAFA`, détail du calcul en lignes (ajustements tonés vert/rouge, taux final violet), formule. **Logique de calcul 100 % PRÉSERVÉE** : `calculateFinalRate`/`getBaseRate`/`convertCNYtoXAF`, `result` memo, `handleCurrencySwitch` (conversion XAF↔CNY 2 itérations), `minAmountCNY`, `emptyMessage`, raccourcis. type-check + build verts.
- M5 (3/3) ✅ **Module Taux terminé.** Onglets **Graphique (+`MultiCurveChart`)** et **Historique (+`RateHistoryCard`)** sur le kit. `RateChartTab` : sélecteur période en pilules, conteneurs en cartes, erreur `ScreenError`. `MultiCurveChart` : **Recharts CONSERVÉ** (AreaChart/Area/XAxis/YAxis/Tooltip/gradients) — seuls les conteneurs passent aux cartes du kit, les couleurs d'axes/grille adoucies (gris neutre lisible clair+sombre), `pm.chartColor` des courbes intact ; stats Min/Moy/Max sur cartes (tons rouge/ambre/vert), toggles de courbes en pilules (ring `chartColor`), écart entre modes avec `MethodLogo` + barres `chartColor` ; logique `chartData`/`stats`/`visibleLines`/`toggleLine`/écart vs Cash intacte. `RateHistoryTab` : états load/erreur/vide via kit. `RateHistoryCard` : carte douce (ring violet si actif), en-tête date + **`StatusPill` « Actif »** + pilule variation tonée, grille 4 modes avec **vrais logos** + gros chiffres ; calcul variation (cash vs précédent) intact. type-check + build verts. **Note** : la sous-tâche « porter le flyer v3 dans generate-flyer Satori » n'a PAS été touchée (hors du périmètre de ce run, qui ciblait les écrans Taux UI ; le `RateFlyer` preview + exports PNG/PDF existants sont conservés tels quels dans `RateSetTab`).
- **M5 cochée** : les écrans Taux (shell, Définir, Config, Simulateur, Graphique, Historique + composants `SimulatorResult`/`MultiCurveChart`/`RateHistoryCard` + helper `MethodLogo`) sont sur le kit, logique RPC/calcul/Recharts 100 % préservée. V1 (`src/pages/rates/*`) NON touchés.

## Résumé matin
_(à remplir en fin de run)_
