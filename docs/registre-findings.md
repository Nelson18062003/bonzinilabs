# Registre des findings — audit produit

Format : `id | module | catégorie | sévérité | preuve | confiance | statut | vérification`.
Statuts : ouvert · corrigé · proposé (attend validation) · wontfix.
Sévérités : P0 parcours cassé / argent faux / perte de données · P1 logique ou données fausses,
navigation cassée, responsive cassé · P2 état manquant, friction · P3 polish.

## Corrigés avant l'ouverture du registre (passes 25–31, branche `claude/bonzini-cargo-logistics-dcxcys`)
| id | module | catégorie | sév. | preuve | confiance | statut | vérification |
|---|---|---|---|---|---|---|---|
| F-001 | Paiements | argent / concurrence | P0 | `supabase/migrations/20260914091000_terminal_status_guards.sql` : `SELECT * INTO v_wallet … ;` sans `FOR UPDATE` dans les deux branches de `process_payment` → `ledger_entries.balance_before` périmé sous concurrence | HAUT | corrigé (migration `20260914093000`, reportée dans le consolidé) | test `moneyRpcGuards.test.ts` (aiguille `WHERE user_id = v_payment.user_id FOR UPDATE`) |
| F-002 | Paiements | logique métier | P1 | `MobileNewPayment.tsx` : `setClient`/`setMode` ne remettaient pas `selectedBenef` à zéro → paiement créé avec le bénéficiaire d'un autre client | HAUT | corrigé (`chooseClient`/`chooseMode`) | relecture ; `benef4Valid` dépend du bénéficiaire du client courant |
| F-003 | Paiements | calcul | P1 | `rate = parseInt(customRateStr) \|\| FALLBACK_RATE` : champ perso vidé → paiement à 11 530 | HAUT | corrigé (`\|\| baseRate` + `customRateValid` bloque « Suivant ») | relecture |
| F-004 | Dépôts | état périmé | P1 | `useEffect([deposit]) → setConfirmedAmount` écrasait la saisie pendant la validation | HAUT | corrigé (effet supprimé ; `openValidate` initialise) | relecture |
| F-005 | Opérations | données | P1 | badge « Opérations » (`useAdminActionableCounts`) ≠ hub (`to_process + pending_correction`) ≠ segments | HAUT | corrigé (`src/lib/actionable.ts`, hub sur les mêmes compteurs) | test `actionableParity.test.ts` |
| F-006 | Statuts | logique métier | P1 | `canPasteProof`, `pasteTarget`, `CashQRCode`, `getDepositSlaLevel` ignoraient `cancelled_by_admin` / `pending_correction` | HAUT | corrigé (`src/lib/terminalStatuses.ts`) | test `terminalStatuses.test.ts` |
| F-007 | Temps réel | données désynchronisées | P1 | `useRealtimeInvalidation.ts` : heuristique `admin-${table}` ne couvrait pas `admin-deposit-proofs`, `deposit-stats`, badges… ; canal client dépendant de `[user]` (mort au refresh du jeton) | HAUT | corrigé (radicaux par table + badges ; `userId` + nom de canal unique) | relecture ; non reproduit en prod (INCONNU) |
| F-008 | Dépôts | pagination | P1 | filtre famille côté client sur une liste paginée serveur → « Aucun dépôt » avec `hasNextPage` | HAUT | corrigé (`params.methods` serveur) | relecture |
| F-009 | i18n | données affichées | P1 | 20 valeurs fr coupées à l'apostrophe (`common.json` : `appTheme: "Thème de l"`, `blockAccess`, `createAdmin`, `exportError`…) | HAUT | corrigé | capture `m/more/settings` |
| F-010 | Support | runtime | P1 | `MobileSupportStatsScreen.tsx` : `stats?.daily_volume.map`, `stats.per_admin.length` → plantage + « undefined » | HAUT | corrigé (`?? []`, `?? 0`) | capture sans `errs` |
| F-011 | Agent cash | i18n | P1 | `AgentCashLogin.tsx` : `language === 'en' ? 'Continue' : '继续'` alors que `language` peut être `'fr'` | HAUT | corrigé (`pick(en, zh, fr)`) | capture `a/login` |
| F-012 | Dépôts | autorisation UI | P2 | fiche dépôt sans `hasPermission('canProcessDeposits')` (le serveur refusait déjà) | HAUT | corrigé | relecture |
| F-013 | Paiements | validation | P1 | `BulkPaymentCreate.tsx` : `amountValid = eXaf >= 1` sans plafond ni entier sûr | HAUT | corrigé (`isValidXafAmount` + cap) | relecture |
| F-014 | Paiements | upload | P2 | QR bénéficiaire sans `validateUploadFile` | HAUT | corrigé | relecture |
| F-015 | Dépôts | UX | P2 | `toast.error('Dépôt rejeté')` sur un succès ; journal d'audit bloquant après création | HAUT | corrigé | relecture |
| F-016 | Support | UX | P2 | `confirm()` natif pour supprimer une réponse / suggestion | HAUT | corrigé (feuille « Garder / Supprimer ») | type-check |
| F-017 | Kit | responsive / a11y | P2 | 37 px chips, 16 px œil, 26 px bascule thème, 8 px marqueurs, curseur 24 px (`index.css:1626` règle mobile) | HAUT | corrigé | audit `tools/audit-mobile.mjs` : 0 cible < 40 hors pseudo-éléments |
| F-018 | Clients | validation | P2 | `MobileCreateClient.tsx` : e-mail jamais vérifié, téléphone `length >= 9` sans nettoyage | HAUT | corrigé | type-check |

## Ouverts / proposés
| id | module | catégorie | sév. | preuve | confiance | statut | note |
|---|---|---|---|---|---|---|---|
| F-019 | Mola | autorisation | P1 | outils de lecture exposés à tous les rôles en service-role (`supabase/functions/admin-assistant`) ; `READ_TOOLS.filter((t) => t.always \|\| perms[t.permission])` rétablirait la garde | MOYEN | proposé | décision produit : Mola doit-il lire tout pour tous ? |
| F-020 | Infra | dette | P2 | edge functions `create-admin`, `create-agent`, `create-client` supprimées du dépôt mais toujours déployées | INCONNU (pas d'accès prod) | proposé | à retirer manuellement |
| F-021 | Paiements | données | P2 | `useAdminPayments.ts` : deux `UPDATE` directs sur `payments` (repli si `admin_update_payment_beneficiary` absente, effacement du QR) contournent la timeline serveur | MOYEN | ouvert | couvert par RLS ; à migrer vers la RPC |
| F-022 | Paiements | sécurité / argent | **P0** | `20251220211736…sql:84` : policy INSERT `WITH CHECK (auth.uid() = user_id)` jamais retirée ; `20260331000001…sql` : UPDATE client sans borne de colonnes ; aucun `GRANT UPDATE (colonnes)` → un client peut créer un paiement sans débit ou changer `amount_rmb` avant traitement | HAUT (lecture des migrations) / INCONNU en prod (pas d'accès) | **proposé** | migration rédigée : `docs/proposals/2026-09-14-payments-ecritures-directes.sql` ; attend validation (auth/schéma) |
| F-023 | Paiements | parcours cassé | P1 | `MobilePaymentDetailV2.tsx:573` : « Commencer le paiement » proposé sur `cash_scanned`, mais `process_payment` (`20260914093000…sql:40`) refuse hors `ready_for_payment` → bouton qui échoue toujours | HAUT | corrigé | `src/lib/paymentActions.ts` + test `paymentActions.test.ts` ; la signature reste le chemin du cash |
| F-024 | Chaîne cash | sécurité | P1 | `20260107115520…sql:27` : policy UPDATE agents cash avec `USING` seul, sans `WITH CHECK` → un agent peut clore un cash sans signature | HAUT | **proposé** | même migration que F-022 ; les écrans agent ne font que SELECT (`useAgentCashPayments.ts:43,117`) |
| F-025 | Dépôts | autorisation | P1 | `create_client_deposit` (`20260819110000…sql`) : aucune liaison `p_user_id` ↔ `auth.uid()` (grep vide dans les deux définitions) → un client peut déclarer un dépôt sur le compte d'un autre | HAUT | **proposé** | `docs/proposals/2026-09-14-depots-et-portefeuille.sql` |
| F-026 | Dépôts | autorisation | P2 | `20251231174812…sql:33` : policy UPDATE `is_admin` sans WITH CHECK ni colonnes ; aucune écriture directe dans le code | HAUT | **proposé** | même proposition (drop) |
| F-027 | Dépôts | statut mort | P3 | `pending_correction` n'est plus posé par aucun geste admin (`MobileDepositDetailV2.tsx:167` « correction flow suppressed ») mais reste un filtre « À corriger » et un statut client | HAUT | ouvert | décision produit : réactiver le geste ou retirer le filtre |
| F-028 | Portefeuille | argent / régression latente | P1 | `20260831160000…sql` : `admin_adjust_wallet` sans `p_amount > 0` et `create_wallet_adjustment` sans `FOR UPDATE` ; corrigés EN PLACE par `20260831200000` / `20260831220000` (live OK), mais toute recopie de la définition complète réintroduit le crédit par montant négatif | HAUT (dépôt) / MOYEN (live, non vérifiable) | corrigé (garde-fou) | `moneyRpcGuards.test.ts` exige `FOR UPDATE` et `p_amount <= 0` dans toute redéfinition postérieure |
| F-029 | Portefeuille | validation | P2 | `AdjustmentDrawer.tsx:48` : `isValid = amount > 0` sans plafond ni entier sûr (dépôts/paiements en ont un) | HAUT | corrigé | `isValidXafAmount` + message « dépasse 50 000 000 XAF » |
| F-030 | Portefeuille | données tronquées | P2 | `useClientManagement.ts:224` : grand livre client `.limit(1000)` sans pagination → au-delà, les écritures anciennes sont invisibles sans message | HAUT | corrigé | `useClientLedgerPaged` (pages de 100, `InfiniteScrollTrigger`) + `useClientLedgerCount` pour « Voir tout (N) » ; test `ledgerPaging.test.ts` ; capture `m/clients/u5/ledger` |
| F-031 | App client / Portefeuille | dérive de schéma | P1 | aucune policy SELECT propriétaire sur `wallets` dans le dépôt (retirée `20251217203856`, jamais recréée ; grep insensible à la casse vide) alors que `useWallet.ts:38` lit la table en direct | MOYEN (dépôt HAUT, prod INCONNU) | **proposé** | `docs/proposals/2026-09-14-clients-et-politiques-manquantes.sql` (policy idempotente) |
| F-032 | Clients | faux positif | — | `MobileClientScan.tsx:67` appelle bien `find_client_by_customer_code` (l'affichage `rpc('n')` venait d'un `rg -r n`) | HAUT | wontfix | vérifié par `sed` |
| F-033 | Clients | perte de données / argent | **P0** | `admin_delete_client` (`20260831160000…sql`) supprime grand livre, dépôts et paiements sans vérifier solde ni opérations en cours ; seules les UI le font (`MobileClientDetail.tsx:217-234`) | HAUT | **proposé** | même proposition (gardes serveur + aiguille de test) |
| F-034 | Clients | autorisation | P2 | `admin_create_client` (`20260301240000…sql:26`) et `admin_setup_client` (`20260221000000…sql:18`) gardées par `is_admin` seul | HAUT | **proposé** | même proposition ; vérifier `ops` ⊇ canManageUsers avant |
| F-035 | Auth admin | — | — | connexion OTP / mot de passe, `is_admin` et `admin_has_permission` filtrent `is_disabled`, contexte refuse un admin désactivé (`AdminAuthContext.tsx:246`), `ProtectedAdminRoute` redirige ; `is_support_admin` filtre aussi | HAUT | vérifié, rien à signaler | — |
| F-036 | Cargo / Taux / Trésorerie / Support | — | — | RPC gardées (`canManageCargo`/`canViewCargo`, `can_manage_rates` + montants > 0, `can_access_treasury` + montants > 0, `is_support_admin`), aucune écriture directe sur leurs tables depuis l'admin | HAUT | vérifié, rien à signaler | — |
| F-037 | Mola | autorisation | P2 | `assistant_readonly_query` gardée par `is_admin` seul (lecture) — cohérent avec F-019 (décision produit) | HAUT | ouvert | regrouper avec F-019 |
| F-038 | App client | responsive / troncature | P2 | `OperationsList.tsx:44`, `HistoryPage.tsx:200-201` : libellés tronqués (« Dépôt BZ-DP-2026-0… ») ; `QuickActions.tsx:29` : « Bénéficiaires » débordait de sa tuile ; `ClientTabBar.tsx:32` : onglets « Paiements » / « Historique » qui se touchaient ; `PaymentsPage.tsx:139` : « 1 000 000 XAF » coupé au milieu ; `PaymentsPage.tsx:231-235`, `DepositsPage.tsx:202` : cartes de liste tronquées à 320 px | HAUT (captures `tools/audit-client.mjs`) | corrigé | recapture wallet / payments / history / deposits à 390 et 320 px : 0 débordement, 0 troncature |
| F-039 | App client | texte | P3 | `fr/deposits.json:162,175` : « code marchand affiché ci-dessous » alors que le code est au-dessus des étapes | HAUT | corrigé | « ci-dessus » |
| F-040 | Outillage | — | — | nouveau harnais `tools/audit-client.mjs` : session client factice (`bonzini-client-auth`), `/auth/v1/user` et `/auth/v1/token` répondus en local, fixtures partagées ; 14 écrans de l'app importateur capturables sans compte | HAUT | fait | à lancer contre `npx vite --port 8094` (sans alias de mock) |
| F-041 | App client | état manquant | P2 | `DepositsPage.tsx:168`, `PaymentsPage.tsx:196`, `HistoryPage.tsx:178` : une requête en échec tombait dans l'état vide (« Aucun dépôt ») — un client hors réseau croyait n'avoir aucune opération | HAUT (reproduit : `deposits` en `connectionfailed` → « Aucun dépôt ») | corrigé | `QueryError` (message + Réessayer) ; repro après fix : « Impossible de charger vos dépôts. » |
| F-042 | Admin mobile | état manquant | P2 | `MobileDepositsScreenV2.tsx:325`, `MobilePaymentsScreen.tsx:391`, `MobileClientsScreen.tsx:102`, `MobileCargoScreen.tsx:162`, `MobileNotificationsScreen.tsx:71`, `MobileSupportListScreen.tsx:152` : aucun `isError` — une panne se lisait « Aucun dépôt / client / conteneur » | HAUT | corrigé | `QueryError` avec « Réessayer » sur les six listes ; captures sans erreur de page |
| F-043 | Admin desktop | — | — | 14 écrans desktop capturés à 1280 px (`tools/_desk.tmp.mjs` = audit-mobile en 1280×800) : 0 erreur de page, 0 débordement du document ; les `overflow-auto` signalés sont les panneaux défilants voulus | HAUT | vérifié, rien à signaler | typographie dense (12–13 px) assumée sur desktop |
| F-044 | App client | typographie | P3 | textes sous 12 px sur `/rates` (tuiles de méthode 10 px, « Vous envoyez » 11 px, graduations du graphique 10 px), unité « XAF » 10 px, onglets 10 px | HAUT | ouvert (décision) | design propre à l'app client ; à trancher avec la cible importateurs avant de relever |
| F-045 | App client | accessibilité / cible | P2 | `DepositDetailPage.tsx:369` : bouton de suppression de preuve de 24 px sans nom accessible (geste destructif) ; `RateConverter.tsx:108` : bouton d'inversion sans nom | HAUT (scan runtime des boutons sans nom sur 22 écrans : 3 trouvés) | corrigé | `aria-label` + cible 44 px ; re-scan : 0 |
| F-046 | i18n | parité | P3 | en/zh : seule `bulk.errAmountCap` manquait par rapport au français (clé ajoutée aujourd'hui) ; aucune valeur vide ni laissée en français | HAUT | corrigé | script de parité fr/en/zh |
| F-047 | App client | validation | — | `paymentRateLogic.ts:115` : montant `> 0`, `≤ 50 000 000`, entier sûr, solde vérifié ; `NewDepositPage.tsx:68` plafonné ; `isBeneficiaryFormValid` sur le carnet | HAUT | vérifié, rien à signaler | — |

